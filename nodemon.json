{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "ts-node --transpile-only src/app.ts",
  "env": {
    "NODE_ENV": "development",
    "DATABASE_URL": "postgresql://bitespeed_user:bitespeed_password@localhost:5432/bitespeed_db"
  }
} 